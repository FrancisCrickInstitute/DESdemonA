#!/bin/sh
ml use /camp/apps/misc/stp/babs/manual/modules/all
module_array=(R/3.6.0-foss-2016b-BABS)
for i in "${module_array[@]}"
do
	module load $i
done
[[ $0 != '-bash' ]] && [[ $0 != '/bin/sh' ]] && R "$@"


